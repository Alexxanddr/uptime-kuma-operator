apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-app
  namespace: default
  annotations:
    uptime-kuma.io/enabled: "true"
    uptime-kuma.io/name: "Sample App Production"
    uptime-kuma.io/type: "http"
    uptime-kuma.io/url: "https://my-service.default.svc.cluster.local"
    uptime-kuma.io/status-codes: "200-299, 301, 302"
    uptime-kuma.io/interval: "60"
    uptime-kuma.io/retries: "3"
    uptime-kuma.io/notifications: "Slack Alerts, Email Admins"
    uptime-kuma.io/group: "Web Services"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sample-app
  template:
    metadata:
      labels:
        app: sample-app
    spec:
      containers:
      - name: nginx
        image: nginx:latest
